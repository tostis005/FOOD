name: Final clean pass articles 1-25

on:
  push:
    branches:
      - content/final-clean-pass-001-025
    paths:
      - 'content/tools/clean-article-copy.py'
      - '.github/workflows/final-clean-pass.yml'

permissions:
  contents: write

jobs:
  clean:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: content/final-clean-pass-001-025

      - name: Clean all 50 article JSON files
        run: python3 content/tools/clean-article-copy.py

      - name: Validate JSON and article count
        run: |
          python3 - <<'PY'
          import json
          from pathlib import Path
          paths = sorted(Path('content/articles').glob('*/*.json'))
          assert len(paths) == 50, len(paths)
          for p in paths:
              json.loads(p.read_text(encoding='utf-8'))
          print('VALIDATED_ARTICLES=50')
          PY

      - name: Commit clean article set
        run: |
          if git diff --quiet -- content/articles; then
            echo 'No article changes to commit'
            exit 0
          fi
          git config user.name 'github-actions[bot]'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          git add content/articles
          git commit -m 'Editorial clean pass for articles 1-25 in ES and EN'
          git push origin HEAD:content/final-clean-pass-001-025
